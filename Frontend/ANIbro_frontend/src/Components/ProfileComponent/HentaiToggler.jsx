import React, { useContext } from 'react'
import { typeContext } from '../../Contexts/TypeContext'

const HentaiToggler = () => {
    const [] = useContext(typeContext)
  return (
    <div>
      
    </div>
  )
}

export default HentaiToggler
